# 🎯 Портфолио - Bat файлы для автоматизации

## 📋 Описание bat

Набор bat файлов для автоматизации работы с сайтом-портфолио. Удобная замена ручного ввода команд в терминале.

## 🚀 Основные файлы

### 1. **portfolio-manager.bat** - Главное меню
**Рекомендуемый способ запуска!** Интерактивное меню для всех операций.
```bash
portfolio-manager.bat
```

### 2. **start-dev.bat** - Запуск dev-сервера
Запускает локальный dev-сервер на http://localhost:3000
```bash
start-dev.bat
```

### 3. **build-production.bat** - Сборка для продакшна
Создает оптимизированную сборку для деплоя
```bash
build-production.bat
```

### 4. **update-data.bat** - Обновление JSON данных
Копирует обновленные JSON файлы из папки `../Data/`
```bash
update-data.bat
```

### 5. **clean-all.bat** - Полная очистка
Удаляет все кэши и зависимости для чистой установки
```bash
clean-all.bat
```

### 6. **check-status.bat** - Проверка состояния
Показывает текущее состояние проекта и активные процессы
```bash
check-status.bat
```

## 🎯 Быстрый старт

### Для ежедневной работы:
1. Запустите `portfolio-manager.bat`
2. Выберите пункт 1 "Запустить dev-сервер"
3. Откройте http://localhost:3000 в браузере

### При изменении данных:
1. Обновите файлы в папке `../Data/`
2. Запустите `update-data.bat`
3. Данные автоматически обновятся на сайте

### При проблемах:
1. Запустите `clean-all.bat`
2. Затем `start-dev.bat`

## 📁 Структура проекта

```
Frontend/
├── portfolio-manager.bat     # 🎯 ГЛАВНОЕ МЕНЮ
├── start-dev.bat            # 🚀 Запуск dev-сервера
├── build-production.bat     # 🏗️ Сборка для продакшна
├── update-data.bat         # 📄 Обновление JSON
├── clean-all.bat           # 🧹 Полная очистка
├── check-status.bat        # 📊 Проверка состояния
├── app/                    # Next.js приложение
├── components/             # React компоненты
├── public/
│   ├── data/              # JSON файлы данных
│   │   ├── projects.json   # Проекты с переводами
│   │   ├── services.json    # Услуги с переводами
│   │   └── testimonials.json # Отзывы с переводами
│   └── assets/            # Изображения и ресурсы
└── BAT_FILES_README.md     # Этот файл

Data/
├── projects.json           # Проекты с переводами
├── services.json           # Услуги с переводами
└── testimonials.json       # Отзывы с переводами
```

## 🔧 Что делают bat файлы

### start-dev.bat:
- Останавливает зависшие процессы Node.js
- Очищает кэш Next.js (.next)
- Проверяет зависимости (устанавливает если нужно)
- Запускает dev-сервер на порту 3000

### build-production.bat:
- Останавливает dev-сервер
- Очищает предыдущие сборки
- Выполняет `npm run build`
- Создает оптимизированную сборку

### update-data.bat:
- Проверяет наличие исходных JSON файлов в `../Data/`
- Копирует projects.json, services.json и testimonials.json в `public/data/`
- Обновляет данные на сайте
- **Проверяет валидность JSON** синтаксиса
- **Создает примеры файлов** если исходные не найдены

### clean-all.bat:
- Останавливает все процессы Node.js/npm
- Удаляет node_modules, .next, out
- Очищает npm кэш
- Подготавливает проект к чистой установке

### check-status.bat:
- Проверяет наличие всех ключевых файлов
- Показывает размеры JSON файлов
- Проверяет активные процессы
- Показывает занятость портов

## 🌍 Многоязычность JSON данных

### Структура projects.json:
```json
{
  "id": "unique-project-id",
  "title": {
    "ru": "Название на русском",
    "en": "Title in English"
  },
  "description": {
    "ru": "Описание на русском языке",
    "en": "Description in English"
  },
  "stack": "Technology, Stack, Here",
  "tag": "https://github.com/username/repo"
}
```

### Структура services.json:
```json
{
  "id": "unique-service-id",
  "title": {
    "ru": "Название услуги на русском",
    "en": "Service title in English"
  },
  "description": {
    "ru": "Описание услуги на русском",
    "en": "Service description in English"
  }
}
```

### Структура testimonials.json:
```json
[
  {
    "text": {
      "ru": "Текст отзыва на русском языке",
      "en": "Testimonial text in English"
    },
    "author": {
      "ru": "Имя автора на русском",
      "en": "Author name in English"
    },
    "role": {
      "ru": "Должность на русском",
      "en": "Role in English"
    }
  }
]
```

### ⚠️ Важные правила:
1. **Обязательно указывайте оба языка**: `ru` и `en`
2. **fallback**: если перевод для текущего языка не найден, используется русский
3. **ID должны быть уникальными** для каждого проекта/услуги
4. **JSON должен быть валидным** (используйте онлайн-валидатор)
5. **Для отзывов** можно добавлять любое количество элементов в массив

## 💬 Управление отзывами

### Текущее состояние:
- ✅ Отзывы отображаются в красивом карусельном интерфейсе
- ✅ Автоматические инициалы из имени автора
- ✅ 5-звёздочный рейтинг для каждого отзыва
- ✅ Поддержка прокрутки (стрелки навигации)
- ✅ Адаптивный дизайн для мобильных устройств

### Добавление отзывов:
1. **Пустой массив**: если файл `testimonials.json` содержит `[]`, показывается заглушка "Здесь пока ничего нет"
2. **С отзывами**: при добавлении объектов в массив они автоматически отображаются в карусели
3. **Многоязычность**: каждый отзыв поддерживает переводы на русский и английский

### Пример добавления отзыва:
```json
[
  {
    "text": {
      "ru": "Отличная работа! Создал наш MVP за неделю.",
      "en": "Excellent work! Created our MVP in a week."
    },
    "author": {
      "ru": "Иван Сидоров",
      "en": "Ivan Sidorov"
    },
    "role": {
      "ru": "CEO, StartupCorp",
      "en": "CEO, StartupCorp"
    }
  }
]
```

## ⚡ Горячие клавиши

В главном меню (`portfolio-manager.bat`):
- **1** - Запуск dev-сервера
- **2** - Сборка для продакшна
- **3** - Обновление JSON данных
- **4** - Полная очистка
- **5** - Проверка состояния
- **6** - Выход

## 🆘 Устранение проблем

### Порт 3000 занят:
1. Запустите `check-status.bat`
2. Если процессы активны, выполните `clean-all.bat`
3. Затем `start-dev.bat`

### Ошибки сборки:
1. Выполните `clean-all.bat`
2. Затем `start-dev.bat`

### Не обновляются данные:
1. Проверьте JSON файлы в `../Data/`
2. Запустите `update-data.bat`
3. Перезагрузите страницу в браузере

### Медленная работа:
1. Выполните `clean-all.bat`
2. Перезапустите dev-сервер

### Ошибки JSON:
1. Проверьте синтаксис JSON в онлайн-валидаторе
2. Убедитесь что указаны оба языка (`ru` и `en`)
3. Проверьте уникальность ID (для projects и services)
4. Для testimonials проверьте, что это валидный массив

### Отзывы не отображаются:
1. Проверьте `../Data/testimonials.json`
2. Убедитесь что это массив объектов
3. Проверьте наличие переводов на оба языка
4. Запустите `update-data.bat`

## 💡 Советы

- ✅ Всегда используйте `portfolio-manager.bat` для удобства
- 🔄 JSON данные обновляются автоматически при изменении файлов
- 📱 Для деплоя используйте `build-production.bat`
- 🧹 Периодически выполняйте `clean-all.bat` для оптимизации
- 📊 Регулярно проверяйте состояние через `check-status.bat`
- 🌍 Поддерживайте переводы на оба языка в актуальном состоянии
- 🎯 Используйте понятные и уникальные ID для проектов и услуг
- 💬 Отзывы можно добавлять постепенно, начиная с пустого массива
- ⭐ Каждый отзыв автоматически получает 5-звёздочный рейтинг
- 🎠 Карусель отзывов автоматически адаптируется к количеству элементов