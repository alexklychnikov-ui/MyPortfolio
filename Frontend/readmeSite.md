# AI-агент: Анализ Frontend-проекта (Портфолио No-Code & AI Разработчика)

## 📋 Общее Описание

**Тип проекта:** Персональный сайт-портфолио No-Code & AI разработчика

**Разработчик:** Александр Клычников

**Назначение:** Демонстрация навыков, проектов и услуг No-Code разработки с фокусом на:
- Быстрая разработка MVP
- Автоматизация бизнес-процессов
- Интеграция AI-решений

**URL/Местоположение:** Папка `Frontend/`

---

## 🏗️ Архитектура и Технологии

### Фреймворк и Рендеринг
- **Платформа:** Next.js 16.1.6 (App Router)
- **Версия React:** 19
- **Рендеринг:** Client Components (`"use client"`)
- **Стилизация:** Tailwind CSS 3.4 + кастомный CSS (portfolio.css)
- **Шрифт:** Inter (Google Fonts via next/font/google)

### UI Компоненты
- **Библиотека UI:** shadcn/ui (Radix UI primitives)
- **Иконки:** Lucide React
- **Анимации:** Tailwind CSS Animate
- **Карусель:** Embla Carousel React
- **Уведомления:** Sonner / Toast
- **Графики:** Recharts
- **Формы:** React Hook Form + Zod

### Управление Состоянием и i18n
- **Локализация:** Кастомный i18n провайдер (`components/portfolio/i18n.tsx`)
- **Поддержка языков:** EN, RU (переключатель в навбаре)
- **Утилиты:** clsx, tailwind-merge, class-variance-authority (CVA)

### Динамическая загрузка данных
- **Проекты:** Загружаются из `public/data/projects.json` через `fetch()`
- **Услуги:** Загружаются из `public/data/services.json` через `fetch()`
- **Отзывы:** Загружаются из `public/data/testimonials.json` через `fetch()`

### Конфигурация
- **TypeScript:** 5.7.3 (строгий режим, но ignoreBuildErrors: true в next.config)
- **Lint:** ESLint
- **PostCSS:** с @tailwindcss/postcss
- **Изображения:** Не оптимизированы (unoptimized: true в next.config)

---

## 📁 Структура Файлов

```
Frontend/
├── app/
│   ├── layout.tsx           # Root layout (метаданные, viewport)
│   ├── page.tsx             # Главная страница
│   ├── globals.css          # Tailwind + CSS переменные
│   └── portfolio.css        # Кастомные стили портфолио
├── components/
│   ├── portfolio/           # Компоненты секций портфолио
│   │   ├── Hero.tsx         # Секция "Обо мне" + фото
│   │   ├── Navbar.tsx       # Навигация + переключатель языка
│   │   ├── Skills.tsx       # Навыки (табы: No-Code, AI, Automation)
│   │   ├── Projects.tsx     # Портфолио проектов (карусель)
│   │   ├── Services.tsx     # Услуги (3 карточки)
│   │   ├── Testimonials.tsx # Отзывы (заглушка при пустом JSON)
│   │   ├── Contact.tsx      # Форма контактов + инфо
│   │   └── i18n.tsx         # Локализация (RU/EN)
│   └── ui/                  # shadcn/ui компоненты (40+ файлов)
├── hooks/
│   ├── use-mobile.tsx       # Хук определения мобильного устройства
│   └── use-toast.ts         # Хук для уведомлений
├── lib/
│   └── utils.ts             # Утилиты (cn = clsx + tailwind-merge)
├── public/
│   ├── data/                # JSON файлы с контентом
│   │   ├── projects.json    # Проекты (5 шт.)
│   │   ├── services.json    # Услуги (3 шт.)
│   │   └── testimonials.json # Отзывы (пока пустой [])
│   └── assets/profile.jpg   # Фото профиля (340x340px)
├── package.json
├── tailwind.config.ts
├── next.config.mjs
├── tsconfig.json
└── postcss.config.mjs
```

---

## 📄 Структура JSON файлов (public/data/)

### projects.json
**Назначение:** Хранение списка проектов для секции Projects

**Структура элемента:**
```typescript
{
  id: string                    // Уникальный идентификатор (например, "tg-bot-both-memory")
  title: {
    ru: string                  // Название на русском
    en: string                  // Название на английском
  }
  description: {
    ru: string                  // Описание на русском
    en: string                  // Описание на английском
  }
  stack: string                 // Стек технологий через запятую (например, "Python, Telegram Bot API, SQLite, asyncio")
  tag: string                   // Ссылка на GitHub репозиторий
}
```

**Текущие проекты (5 шт.):**
1. `tg-bot-both-memory` — Telegram-бот с короткой и долгой памятью
2. `tg-expenses-tracker` — Унифицированный Telegram-бот для ведения личных расходов
3. `langchain-portfolio-generator` — Генератор сайт-портфолио на базе LangChain
4. `weather-telegram-bot` — Telegram-бот для получения информации о погоде
5. `dynamics-ax-assistant` — Dynamics AX X++ Development Assistant

**Используется в:** `components/portfolio/Projects.tsx` (загружается через `fetch('/data/projects.json')`)

---

### services.json
**Назначение:** Хранение списка услуг для секции Services

**Структура элемента:**
```typescript
{
  id: string                    // Уникальный идентификатор (например, "mvp-development")
  title: {
    ru: string                  // Название услуги на русском
    en: string                  // Название услуги на английском
  }
  description: {
    ru: string                  // Описание на русском
    en: string                  // Описание на английском
  }
}
```

**Текущие услуги (3 шт.):**
1. `mvp-development` — Разработка MVP
2. `workflow-automation` — Автоматизация процессов
3. `ai-integration` — Интеграция AI

**Используется в:** `components/portfolio/Services.tsx` (загружается через `fetch('/data/services.json')`)

---

### testimonials.json
**Назначение:** Хранение отзывов клиентов для секции Testimonials

**Структура элемента:**
```typescript
{
  text: {
    ru: string                  // Текст отзыва на русском
    en: string                  // Текст отзыва на английском
  }
  author: {
    ru: string                  // Имя автора на русском
    en: string                  // Имя автора на английском
  }
  role: {
    ru: string                  // Должность/компания на русском
    en: string                  // Должность/компания на английском
  }
}
```

**Текущее состояние:** Пустой массив `[]` — отображается заглушка "В этом разделе пока нет информации"

**Используется в:** `components/portfolio/Testimonials.tsx` (загружается через `fetch('/data/testimonials.json')`)

---

## 🎨 Дизайн и UI/UX

### Цветовая Палитра (CSS Variables)
```css
--color-bg: #f5f7fa           /* Фон */
--color-surface: #ffffff      /* Поверхности */
--color-text: #1a1d23         /* Основной текст */
--color-text-secondary: #5a6170 /* Вторичный текст */
--color-primary: #2563eb      /* Акцент (синий) */
--color-accent: #0d9488       /* Доп. акцент (зеленый) */
--color-border: #e2e6ed       /* Границы */
--color-badge-bg: #eef2ff     /* Фон бейджей */
--color-badge-text: #3b5bdb   /* Текст бейджей */
```

### Секции Страницы (порядок сверху вниз)
1. **Navbar** — фиксированная навигация с переключателем языков
2. **Hero** — заголовок, описание, список преимуществ, CTA-кнопки, фото
3. **Skills** — 3 таба (No-Code, AI/API, Automation) с бейджами инструментов
4. **Projects** — карусель с карточками проектов (загружается из JSON)
5. **Services** — 3 карточки услуг с иконками (загружается из JSON)
6. **Testimonials** — заглушка при пустом JSON, карусель при наличии данных
7. **Contact** — контактная информация + форма
8. **Footer** — копирайт

### Адаптивность (Breakpoints)
- **Desktop (>900px):** 2 колонки в Hero, 3 колонки в Projects/Services
- **Tablet (640-900px):** 2 колонки в Projects/Services
- **Mobile (<640px):** 1 колонка, мобильное меню-бургер

### Анимации
- `accordion-down/up` — для Radix UI компонентов
- Hover-эффекты на карточки и кнопки
- Плавные переходы (0.3s ease)

---

## 🔧 Ключевые Компоненты

### Hero.tsx
```typescript
// use client
- Заголовок: "Привет, я Александр 👋 Собираю продукты из идей, no-code и AI — быстро, аккуратно и с результатом."
// Список преимуществ (3 пункта)
// Кнопки: "Смотреть проекты" + "Связаться"
// Фото: /assets/profile.jpg (340x340, border-radius: 50%)
```

### Navbar.tsx
```typescript
// use client
- Фиксированная позиция при скролле (scrolled class)
// Переключатель языков RU/EN (state: locale)
// Мобильное меню (hamburger toggle)
// Ссылки на секции: #skills, #projects, #services, #testimonials, #contact
```

### Skills.tsx
```typescript
// use client, useState для табов
// 3 таба:
- No-Code: Lovable, Bubble, Webflow, Framer, Airtable, Notion
- AI / API: OpenAI API, Cursor, Claude API, LangChain, Pinecone, Supabase
- Automation: Make, n8n, Zapier, Telegram Bots, Webhooks, Cron Jobs
```

### Projects.tsx
```typescript
// use client
- Загружает данные из /data/projects.json через fetch()
- Карусель на основе embla-carousel
- Кнопки навигации влево/вправо
- Адаптивное количество карточек: 3 на desktop, 2 на tablet, 1 на mobile
- 5 проектов с описанием, стеком технологий и ссылкой на GitHub
```

### Services.tsx
```typescript
// use client
- Загружает данные из /data/services.json через fetch()
- 3 услуги с иконками и описанием
- Кнопка "Обсудить" с якорем на #contact
```

### Testimonials.tsx
```typescript
// use client
- Загружает данные из /data/testimonials.json через fetch()
- Если массив пустой — показывает заглушку "В этом разделе пока нет информации"
- Если есть данные — показывает карусель с отзывами
```

### Contact.tsx
```typescript
// use client
- Информация:
  - Email: alexandr_klychnikov@mail.ru
  - Telegram: Alexandr_Kl (ссылка на t.me/Alexandr_Kl)
  - Локация: Удалённо / По всему миру
  - Время ответа: Обычно отвечаю в течение 12ч
- Форма: name, email, message (нативный HTML form)
- aria-labels для доступности
```

### i18n.tsx
```typescript
// use client, createContext, useState
// Полная локализация всех текстов на EN и RU
// Функция useI18n() для доступа к переводам
// Структура: t.[section].[key][locale]
```

---

## 📦 Зависимости (package.json)

### Core
- `next: 16.1.6`
- `react: ^19`, `react-dom: ^19`

### UI (Radix UI)
- `@radix-ui/react-*` (accordion, dialog, dropdown, tabs, toast, tooltip, etc.)
- `class-variance-authority`, `clsx`, `tailwind-merge`

### Формы и Валидация
- `react-hook-form`, `zod`, `@hookform/resolvers`

### AI и Данные
- `openai`, `langchain`, `recharts`

### Карусель
- `embla-carousel-react`

### Утилиты
- `lucide-react` (иконки), `date-fns`

---

## 🚀 Скрипты

```bash
npm run dev      # Запуск dev-сервера
npm run build    # Production сборка
npm run start    # Запуск production сервера
npm run lint     # ESLint проверка
```

---

## ⚙️ Конфигурация

### next.config.mjs
```javascript
typescript: { ignoreBuildErrors: true }
images: { unoptimized: true }
```

### tailwind.config.ts
- Dark mode: ['class']
- Кастомные цвета (background, foreground, primary, chart, sidebar)
- Кастомные анимации (accordion-down/up)
- Плагин: tailwindcss-animate

---

## 🎯 Ключевые Особенности для AI-Агента

1. **Full-stack Next.js** с App Router и клиентскими компонентами
2. **Двуязычность** (EN/RU) через контекст без внешних библиотек
3. **shadcn/ui** как основа UI — компоненты из `components/ui/`
4. **Tailwind CSS** + кастомный CSS для специфичных стилей
5. **Адаптивный дизайн** — 3 брейкпоинта
6. **ARIA-доступность** — роли tablist, tab, tabpanel, aria-labels
7. **Анимации** — hover-эффекты, скролл, переходы
8. **Динамическая загрузка данных** — проекты, услуги и отзывы загружаются из JSON
9. **Структура портфолио** — 7 секций с конкретным контентом

---

## 🔍 Файлы для Изменения Контента

| Задача | Файл |
|--------|------|
| Изменить тексты (EN/RU) | `components/portfolio/i18n.tsx` |
| Изменить стили | `app/portfolio.css` |
| Добавить/удалить проекты | `public/data/projects.json` |
| Изменить навыки | `components/portfolio/Skills.tsx` |
| Добавить/удалить услуги | `public/data/services.json` |
| Добавить отзывы | `public/data/testimonials.json` |
| Изменить контактные данные | `components/portfolio/Contact.tsx` + `i18n.tsx` |
| Заменить фото профиля | `public/assets/profile.jpg` |
| Изменить метаданные | `app/layout.tsx` (metadata, title, description) |

---

## 📌 Важные Замечания

1. **ignoreBuildErrors: true** — TypeScript ошибки не блокируют сборку
2. **Изображения не оптимизированы** — next/image использует unoptimized
3. **Форма контакта** — нативный HTML form без обработчика (action="#")
4. **Нет backend** — статический сайт, данные загружаются из JSON файлов
5. **CSS переменные** — используются в portfolio.css, не в tailwind.config
6. **Контекст i18n** — оборачивает всю страницу в `I18nProvider`
7. **Мобильное меню** — появляется при клике на бургер, скрыто по умолчанию
8. **JSON файлы** — проекты, услуги и отзывы загружаются динамически через `fetch()` при монтировании компонента
9. **testimonials.json** — пока пустой, отображается заглушка
10. **Контакты обновлены** — email: alexandr_klychnikov@mail.ru, Telegram: @Alexandr_Kl