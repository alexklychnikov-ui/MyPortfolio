2026-02-09 00:00  deploy.sh rewritten to bash script calling docker compose -f docker-compose.prod.yml up -d --build; initial auto-deploy debugging on VPS.

2026-03-13 10:15  Prompt: "proverit est li .env v gitignore / proverit utechki". Done: verified .env is gitignored and not tracked in git history; scanned repo for hardcoded tokens; found committed DB credentials in docker-compose.override.yml (commit 84a6a56 and current HEAD), no OpenAI/Telegram/Pinecone/Resend live tokens found outside .env.

2026-03-20 07:03  Prompt: "Поднять и проверить Backend API + Telegram bot, не трогая n8n". Done: fixed Backend/docker-compose.yml to reuse existing portfolio-postgres on shared app network, removed extra postgres service/ports, added Backend/api/prompts files, added nginx /webhook/telegram proxy to portfolio-telegram-bot and reloaded nginx, corrected Backend/.env DATABASE_URL for existing postgres role, set+verified Telegram webhook, fixed OpenAI strict JSON schema in backend (removed unsupported uri format for tag and required id in services), fixed response validation (project tag as string), rebuilt backend-api, smoke/E2E checks passed (health 200, webhook 200, analyze 200); n8n containers remained unchanged/up.

2026-03-20 07:39  Prompt: "сделай gitignore для этого репозитория и собери туда всё необходимое". Done: expanded root .gitignore for monorepo coverage (secrets/.env, Next.js/Node artifacts, Python backend caches/venvs, IDE/OS junk, docker local artifacts, temp dirs), kept example env files trackable and preserved project-specific Frontend/package-lock ignore.

2026-03-20 07:40  Prompt: "да, сделай" (очистка tracked ignored). Done: checked tracked files matching .gitignore via `git ls-files -ci --exclude-standard`; result empty, no files required `git rm --cached`, workspace left unchanged except existing pending edits.

2026-03-20 07:54  Prompt: "сайт не обновился после данных от Telegram-бота". Done: verified root cause (frontend sections read static `/public/data/*.json`, while bot updates DB via backend API only), synced DB data from `http://127.0.0.1:8000/v1/{projects,services}` into `Frontend/public/data/projects.json` and `services.json`, copied updated JSON files into live `nextjs-app` container (`/app/public/data/*`), confirmed live endpoints `https://portfolio.hayklyvibelexy.ru/data/projects.json` and `/data/services.json` now return fresh data.

2026-03-20 07:57  Prompt: "нормализовать tag в backend (если не URL — подставлять repo_url)". Done: implemented normalization in `Backend/api/app/services/portfolio_service.py` before response validation/save: checks `projects[].tag`, keeps valid http/https URLs, replaces non-URL tags with fallback `repo_url` from accepted repositories (index-based, with first URL fallback), rebuilt `backend-api`, verified normalization behavior in container with runtime check.

2026-03-20 08:03  Prompt: "сделай без ручного копирования". Done: implemented automatic DB->static sync pipeline: added `STATIC_EXPORT_DIR` config, created `static_exporter` service writing `projects.json/services.json`, wired export after successful analyze in `github` router, mounted shared `Frontend/public/data` into `backend-api` (`/shared/public/data`) and `nextjs-app` (`/app/public/data`) via compose, updated Backend env/example, rebuilt `backend-api`, recreated `nextjs-app` with base compose, validated auto-update by calling `/v1/github/analyze` (files mtime changed automatically) and confirmed live `/data/projects.json` and `/data/services.json` now return fresh data with no manual `docker cp`.

2026-03-20 08:06  Prompt: "Ошибка API: 401 Unauthorized в Telegram-боте". Done: diagnosed env mismatch between containers (`backend-api` had `API_INTERNAL_KEY=internal_key_AlexandrKentavr4801`, `telegram-bot` still had stale `change-me-in-prod`), recreated only `telegram-bot` to reload `Backend/.env`, verified bot container key updated and internal call `telegram-bot -> backend-api /v1/github/analyze` now returns `200` (401 eliminated).

2026-03-20 08:25  Prompt: "скиллы не обновляются по backend prompt". Done: identified root cause on frontend: `Skills.tsx` used hardcoded arrays (no `/data/skills.json` read), added dynamic loading from `public/data/skills.json` with fallback defaults; extended backend static export pipeline to include skills (`skills.json`) alongside projects/services by exporting DB `skills` after successful analyze; added new `Frontend/public/data/skills.json` file; rebuilt `backend-api` and rebuilt `nextjs-app` image to include new public asset route; verified live endpoint `https://portfolio.hayklyvibelexy.ru/data/skills.json` returns JSON and synced current skills from backend DB into it.

2026-03-20 09:13  Prompt: "пусть все таблицы автоматически очищаются после получения списка и заполняются новыми данными". Done: implemented strict refresh flow in backend analyze route: added `clear_all()` repository helper and now `/v1/github/analyze` clears `projects/services/skills` immediately on request, exports empty static data, then fills tables with newly generated data and exports fresh static files on success; rebuilt `backend-api`, verified service health.

2026-03-20 09:38  Prompt: "нужно чтобы всё отработало в одной транзакции + обновить формат сообщений бота". Done: switched analyze flow to atomic transaction (`with db.begin()` + `replace_all` without internal commit), removed pre-clear behavior so failed analyze no longer empties tables; verified with forced 422 that counts in projects/services/skills remain unchanged after failure; updated telegram bot text format to: first message `Принял N ссылок. Запускаю анализ.`, final message `Завершил: обработано X из Y. Обновлено в БД: ...` with structured counts and skipped line.

2026-03-20 09:43  Prompt: "Ошибка API 422 No repositories were successfully analyzed". Done: diagnosed live root cause as GitHub unauthenticated rate limit exhaustion (`core.remaining=0`) with `GITHUB_TOKEN` absent in backend container; improved diagnostics in backend and bot: github client now detects rate-limit preflight and response headers, portfolio service includes top reason in 422 detail, bot extracts and shows `detail` instead of raw JSON blob; rebuilt `backend-api` and `telegram-bot`, verified API now returns explicit detail: `GitHub API rate limit exceeded (reset at ...)`.

2026-03-20 10:14  Prompt: "Перепиши readme с последними модификациями". Done: rewrote root `README.md` to reflect current architecture and runtime behavior: split JSON UI vs DB/integration contour, documented transactional analyze flow, automatic static export for projects/services/skills, current Telegram bot message format, required envs (including `GITHUB_TOKEN` and `STATIC_EXPORT_DIR`), common incidents (401/422), safe startup commands, webhook/live endpoints, and n8n isolation note.

